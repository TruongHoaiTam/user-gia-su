export const actSetCurrentTeacher = current_teacher => ({
    type: 'SET_CURRENT_TEACHER',
    current_teacher
});

export const actSetCurrentContract = current_contract => ({
    type: 'SET_CURRENT_CONTRACT',
    current_contract
});